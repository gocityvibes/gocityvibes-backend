This is the backend for Go City Vibes chatbot.
To run locally:
1. Run `npm install`
2. Create a `.env` file and add your OPENAI_API_KEY
3. Run `node index.js` or `npm start`
4. Visit http://localhost:3000/
